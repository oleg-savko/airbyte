bounces_stream:
  options:
    name: "bounces"
    primary_key: <TODO>
    url_base: <TODO>
    schema_loader:
      file_path: "./source_sendgrid/schemas/{{name}}.json"
    retriever:
      requester:
        path: <TODO>
        authenticator: <TODO>
      record_selector: <TODO>
    paginator: <TODO>
    stream_slicer: <TODO>

streams:
  - "*ref(bounces_stream)"
check:
  stream_names:
    - "bounces"
